# ADR-OSA-NNNN — <title>

- **Status:** Proposed | Accepted | Superseded by ADR-OSA-NNNN
- **Date:** YYYY-MM-DD
- **Author:** <name / handle>
- **Reviewers:** <names>

## Context

What problem does this ADR address? What is the situation today, and what about it requires a construct-level change rather than an ordinary content edit?

Be specific about which OSA layers this ADR touches and what the current text says.

## Decision

The change being proposed. State it as a positive declaration, not a question.

If the ADR adds, removes, or redefines a layer, state the layer's new authority boundary explicitly. If it changes the governance principle, state the new principle in full.

## Consequences

What changes downstream if this ADR is accepted? Cover at minimum:

- Effect on existing layer files
- Effect on the relationship-to-frameworks doc (does any external-framework mapping need to change?)
- Effect on the relationship-to-ologos doc (does any Ologos-internal construct positioning shift?)
- Migration burden for adopters who have already cited the prior version

## Alternatives considered

What other ways of solving the same problem were weighed and not chosen, and why? An ADR without alternatives is weaker — it does not show the design space.

## References

- Issues, PRs, prior ADRs, external papers, frameworks cited
- Any cross-fleet register entries (e.g., `cross-ai/reference/.../ADR-...md`) that informed or are informed by this decision
